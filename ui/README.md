# User interfaces

Canonical inventory: `registry.yaml`.

The initial `ui_status: none` state is explicit. Before adding the first UI,
change the status and register a candidate surface with its purpose, source,
run path, data boundary, lifecycle, and verification command.
